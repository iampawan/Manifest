{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "manifest": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/tools/manifest-mcp/server.mjs"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
