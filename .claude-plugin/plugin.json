{
  "name": "manifest",
  "version": "0.26.6",
  "description": "PRD-to-production pipeline with quality gates. Author contracts, run critics, implement via agent, verify deploys, and produce post-launch reports \u2014 all from markdown in your repo.",
  "author": {
    "name": "Pawan"
  },
  "mcpServers": {
    "figma-rest": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/tools/figma-rest-mcp/index.mjs"],
      "env": { "FIGMA_TOKEN": "${FIGMA_TOKEN}" }
    }
  }
}