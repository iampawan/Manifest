## What this changes

A short description of the change and why.

## Type of change

- [ ] Bug fix
- [ ] New feature / capability
- [ ] Docs only
- [ ] Refactor / internal

## Checklist

- [ ] The eval suite is green: `cd scripts && npm install && cd .. && node --test eval/*.test.mjs`
- [ ] Added/updated a test in `eval/` for any change to `scripts/`
- [ ] Updated `CHANGELOG.md` and bumped `version` in `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` (if user-visible)
- [ ] Commit messages follow the conventional style (`feat:` / `fix:` / `docs:` / `chore:`)
- [ ] If a `workflows/*.yml` changed, noted it here so consumers know to re-copy

## Notes for reviewers

Anything that needs extra attention.
